% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setting_specific_ach.R
\name{ach_to_p_inf}
\alias{ach_to_p_inf}
\title{Wells-Riley infection probability per location}
\usage{
ach_to_p_inf(ach_values, parameters_list, setting)
}
\arguments{
\item{ach_values}{Numeric vector of per-location baseline ACH values
(1/hour), as returned by \code{\link[=generate_setting_specific_ach]{generate_setting_specific_ach()}}.}

\item{parameters_list}{A list of model parameters as generated by
\code{\link[=get_parameters]{get_parameters()}}. The Wells-Riley parameters and the setting volume
(\verb{volume_per_person_<setting>}) are read from here.}

\item{setting}{Character. One of \code{"workplace"}, \code{"school"}, \code{"leisure"},
or \code{"household"}. Used to select the appropriate \verb{volume_per_person_*}.}
}
\value{
Numeric vector of per-location infection probabilities, same length
as \code{ach_values}.
}
\description{
Computes the absolute (unnormalised) single-exposure Wells-Riley infection
probability for each location in a setting:

\deqn{p_{inf}(i) = 1 - \exp(-r \cdot C_{ss}(i) \cdot RR_{tv} \cdot t)}
\deqn{C_{ss}(i) = (I \cdot \pi) / (\alpha_i \cdot V), \quad \alpha_i = ACH_i + k_D}

Unlike \code{\link[=convert_ach_to_riskiness]{convert_ach_to_riskiness()}}, the returned values are not normalised
against a within-setting reference, so they are directly comparable across
setting types. This is required when pooling locations from different
settings for joint intervention targeting, where a within-setting
normalisation would make equal values from different settings represent
unequal absolute risk.
}
\seealso{
Other setting-specific ACH: 
\code{\link{convert_ach_to_riskiness}()},
\code{\link{generate_setting_specific_ach}()},
\code{\link{set_default_ach}()},
\code{\link{set_setting_specific_ach}()}
}
\concept{setting-specific ACH}
